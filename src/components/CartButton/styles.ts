import { Link } from "react-router-dom";
import styled from "styled-components";

export const CartButtonWrapper = styled(Link)`
  background-color: #3a3f44;
  border: none;
`;

export const CartButtonImage = styled.img`
  width: 40px;
  height: 38px;
`;
